play.crypto.secret = dummy

play.evolutions.db.default.autoApply = false

logger {
  root = ERROR
  play = INFO
  application = DEBUG
  useless = DEBUG
}

useless {
  core {
    baseUrl = "http://localhost:9000/core"
  }
}
