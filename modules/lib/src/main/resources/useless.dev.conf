application.secret = dummy

applyEvolutions.default = false

logger {
  root = ERROR
  play = INFO
  application = DEBUG
  useless = DEBUG
}

useless {
  core {
    baseUrl = "http://localhost:8999/core"
  }
}
