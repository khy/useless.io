application.secret = dummy

applyEvolutions.default = false

logger {
  root = ERROR
  play = INFO
  application = DEBUG
  useless = DEBUG
}

useless {
  core {
    baseUrl = "http://localhost:9000/core"
  }
}
